setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
    - plan: heroku-redis
      as: REDIS
build:
  docker:
    web:
      dockerfile: docker/Dockerfile
      target: dev_asgi
    worker:
      dockerfile: docker/Dockerfile
      target: dev_worker
